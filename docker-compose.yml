version: '3.8'
services:
  db:
    image: postgres:latest
    container_name: storefront_postgres_db
    restart: unless-stopped
    ports:
      - "5432:5432"
    env_file:
      - .env
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data:
